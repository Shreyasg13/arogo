flask
itsdangerous
gunicorn

# CVE floors (pip-audit 2026-07). The fixed releases of requests/aiohttp
# require Python >=3.10, so the floor only applies there (production runs
# 3.11 — see render.yaml). On 3.9 pip takes the newest available.
requests>=2.33.0; python_version >= "3.10"
requests;            python_version <  "3.10"
urllib3>=2.7.0;      python_version >= "3.10"

# Optional — background job runner (scheduler.py falls back to stdlib without it)
schedule

# Optional — error tracking. A complete no-op unless SENTRY_DSN is set
# (see observability.py); PII/health data is scrubbed before any event is sent.
sentry-sdk

# Optional — Web Push reminders (push disabled gracefully without it)
pywebpush
# pywebpush pulls in aiohttp; floor it past a batch of 2026 CVEs
aiohttp>=3.14.1; python_version >= "3.10"

# Timezone database for zoneinfo. NOT optional: without it ZoneInfo() raises,
# today_iso(tz) silently falls back to the SERVER's date, and every per-user
# "today" quietly becomes the server's — which is exactly the bug that made
# the app escalate doses to family that the user had already taken. Ships the
# data on hosts that don't carry a system tzdb (notably Windows).
tzdata

# Safe XML parsing for user-uploaded Apple Health exports
defusedxml

# Optional — read vitals out of uploaded lab PDFs (lab_parse degrades to a
# plain "can't read this" message without it; scans/photos need OCR, which
# needs a system binary we don't ship, so they're out of scope either way)
pypdf

# Optional — OCR for prescription-photo import (rx_parse). Text PDFs work
# without it; images light up only when BOTH these AND the system `tesseract`
# binary are installed (e.g. `apt install tesseract-ocr` on a Raspberry Pi).
# Everything degrades to an honest "add by hand" message when absent.
pytesseract
Pillow

# Optional — PostgreSQL backend; activate by setting
#   DATABASE_URL=postgresql://user:pass@host:5432/medeasy
# (default backend is SQLite, no extra install needed)
psycopg2-binary

# Dev / tests
pytest>=8.4.2
pytest-xdist>=3.5.0   # parallel test runs (-n auto --dist loadfile)
