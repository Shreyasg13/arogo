__pycache__/
*.pyc
*.db
*.db-wal
*.db-shm
*.exe
.env
.pytest_cache/
uploads/
*.local.md

# Terraform (deploy/gcp/terraform) — state and vars can hold secrets/IPs
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
override.tf
override.tf.json
.neon

# Neon platform scaffolding (Auth/Buckets/Functions) — not used by the Flask
# app itself, kept local-only per the exploratory neon deploy run
neon.ts
hello.ts
package.json
package-lock.json
node_modules/
skills-lock.json
.claude/skills/
